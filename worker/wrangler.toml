name = "pos-admin"
main = "worker.js"
compatibility_date = "2024-01-01"

# ★ KV Namespace - 部署後需填入 id
[[kv_namespaces]]
binding = "POS_DB"
id = "請填入你的KV_ID"
preview_id = "請填入你的KV_PREVIEW_ID"

# ★ 環境變數
[vars]
# ADMIN_PASSWORD 建議改用 Secrets 設定，不要寫在這裡
# 執行: wrangler secret put ADMIN_PASSWORD
